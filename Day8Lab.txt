Lab 5 - Day8.demo5.restresptoFile
    Spring integration application to invoke RESTAPI and show*System.out) and store data (File Sysetm)
   any input on System.in will invoke service with static parameters
 ----------------------
Lab 4 - Day8.demo4.RESTAPItoSOAP
  Rest API invoking SOAP Service 
  RUn -> http://localhost:8085/first/1/5 (min/max)
  
---------------
Lab3 - Day8.demo3.SOAPConsumer
  use stub objects generated by wsimport and test
-----------------------
Lab 2- Day8.demo2.SOAPProvider
    A simple SOAP Provider offering hello and get employee method
    run -> http://localhost:8080/hello?wsdl

    TO generate stub objects for wsdl
      cmd> set path=jdk\bin
      cmd> wsimport -keep -Xnocompile  http://localhost:8080/hello?wsdl
      generated folder should be in path of SOAP Consume
--------------------------------
Lab 1- 
  intelli - two projects 
   1. Day2.demo1.custom - keep it running -> http://localhost:8085/pv/hello/name
   2nd project (Day8.demo1.restinvoke (copy of Day7.demo3.http)
    Modify ->   gethttpdata method for creating and passing uri variable
        @Bean
    @ServiceActivator(inputChannel = "strchannel")
    public MessageHandler gethttpdata() {
        String url = "http://localhost:8085/pv/hello/{var11}";
        HttpRequestExecutingMessageHandler handler = new HttpRequestExecutingMessageHandler(url);

        Map<String, Expression> mymap = new HashMap<>();
        ///Static Map Values
        mymap.put("var11",new LiteralExpression("aaa") );

        handler.setUriVariableExpressions(mymap);
        handler.setHttpMethod(HttpMethod.GET);
        handler.setRequiresReply(true);
        handler.setExpectedResponseType(String.class);
        handler.setOutputChannelName("httpresp");
        return handler;
    }
  Run -
    create application.properties -> debug=true
  Run -
